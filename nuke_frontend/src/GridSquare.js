import React from "react"

const GridSquare = (props) => {
  return (
    <tr>
      <td>{props.gridsquare.name}</td>
      <td>{props.gridsquare.name}</td>
    </tr>
  )
}

export default GridSquare
